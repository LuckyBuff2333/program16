- 外部接口（AI 日志分析、Jira、知识库）统一通过 `config/config.yaml` 中的 `mock: true/false` 开关切换内置样例与真实 URL，并在各客户端中按相同字段映射读取入参。
- 所有写操作（文档、报表、日志）的路径前缀集中在 `config/config.yaml` 的 `output` 段，代码仅拼接相对路径，不硬编码绝对路径。
- 数据库访问严格只读：CLI 与测试均通过 `src/db.py` 提供的只读查询方法获取 bug_records、已抽查记录与知识库字段，禁止在业务代码中直接写入数据库。