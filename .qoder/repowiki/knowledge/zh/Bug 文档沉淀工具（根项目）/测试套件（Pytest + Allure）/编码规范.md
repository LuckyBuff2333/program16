- 使用 `@allure.feature` 与 `@allure.story` 装饰器对每个测试标注功能域与子场景，形成结构化测试报告。
- 通过 `conftest.py` 中 `autouse=True` 的 fixture 统一注入隔离的数据库与输出路径，所有测试无需自行 setup/teardown。
- 测试数据通过本地辅助函数（如 `_seed_bugids`、`_seed_kb_table`）直接向数据库插入记录，再调用被测接口进行断言。
- CSV 结论报表统一通过 `pandas.read_csv(path, encoding="utf-8-sig")` 读取后以 DataFrame 字段断言业务结果。
- 端到端测试同时断言文件系统产物（文档路径存在、CSV 行数与内容）与数据库状态（查询计数不变），确保无副作用。